{
  "permissions": {
    "allow": [
      "Bash(powershell -NoProfile -Command 'foreach \\($p in 5000,5077,5598,5599\\) { Get-NetTCPConnection -LocalPort $p -State Listen -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue } }')",
      "Bash(timeout 120 npm run verify:bookings)"
    ]
  }
}
